---
- hosts: pgpool
  tasks:
    - name: Menambah User baru untuk akses database
      expect:
        command: pg_md5  -u {{ username }} -p
        responses:
          password: "{{ password }}"

    - name: Reload service pgpool
      service:
        name: pgpool2
        state: reloaded
